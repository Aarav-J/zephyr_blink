/*
 * Device tree overlay for ESP32 DevKit C
 * Defines LED on GPIO2 (typical onboard LED pin)
 */

/ {
	aliases {
		led0 = &led0;
	};

	leds {
		compatible = "gpio-leds";
		led0: led_0 {
			gpios = <&gpio0 2 GPIO_ACTIVE_HIGH>;
			label = "User LED";
		};
	};
};